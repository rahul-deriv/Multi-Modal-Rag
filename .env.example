# Google API key for Gemini models
GOOGLE_API_KEY=your_google_api_key_here

# Models
EMBEDDING_MODEL=models/embedding-001
QUERY_MODEL=gemini-1.5-pro

# Chroma DB settings (optional)
CHROMA_DB_PATH=./chroma_db

# Document processing settings (optional)
MAX_CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Note: File type configuration has been moved to file_config.py.
# Use --file-categories command-line option to specify which categories to process.
# Run 'python main.py --list-categories' to see available options. 